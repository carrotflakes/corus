[package]
name = "wavetables"
version = "0.1.0"
edition = "2021"
description = "wavetable combinator"

[features]
serde = ["dep:serde"]

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
